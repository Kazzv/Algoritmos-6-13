#include <stdio.h>
int main (void) {
    int i, num, suma;
    suma = 0;

    printf ("Ingrese un numero: \n");
    scanf ("%d", &num);

    for (int i = 0; i <= num; i++) {
        suma = suma + i;
    }
    printf ("La suma de los numeros es: %d", suma);
    return 0;
}
